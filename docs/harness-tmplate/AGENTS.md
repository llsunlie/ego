# AGENTS.md - [模块名] 开发指南

## 0. 全局上下文溯源 (Global Context)
⚠️ **重要提示**：如果你是刚刚在本目录下被唤醒的 Agent，请在执行任何代码前，**务必先向上读取项目根目录的 `AGENTS.md`**。
那里包含了不可违反的全局硬约束（例如：Proto 契约规范、防腐层拦截规则），请确保你已加载这些全局上下文。

## 1. 模块定位
[简述本模块在系统中的核心职责，例如：负责处理用户的写作输入与回声匹配。]

## 2. 局部硬约束 (Local Constraints)
- **沙盒隔离**：你当前处于 `internal/[模块名]/` 目录下。除非涉及全局配置或 `proto` 修改，禁止修改此目录以外的代码。
- **依赖倒置**：本模块所需的基础设施能力必须在 `domain/` 或 `app/` 层定义 Port 接口，严禁直接引用 `platform` 的具体实现。
- **数据归属**：本模块拥有对 [相关表名] 表的唯一写入权。严禁越权操作其他模块的数据表。

## 3. 常用开发命令
- **初始化环境**：`sh .harness/init.sh`
- **运行单元测试**：`go test ./internal/[模块名]/...`
- **执行集成测试**：[填写模块特定的测试命令占位]

## 4. 文档路由 (Document Index)
**局部详情（向下看）：**
- **内部设计细节** ➔ 查阅本目录的 `ARCHITECTURE.md`
- **对外暴露接口** ➔ 查阅本目录的 `CONTRACT.md`
- **当前开发任务** ➔ 查阅 `.harness/feature_list.json`
- **恢复开发进度** ➔ 查阅 `.harness/progress.md`

**全局规范（向上看）：**
- **修改跨端 API** ➔ 查阅根目录 `proto/.harness/contract-rules.md`
- **跨模块集成联调** ➔ 查阅根目录 `.harness/integration-rules.md`
- **全局工作流与架构** ➔ 查阅根目录 `docs/` 下的相关专题文档