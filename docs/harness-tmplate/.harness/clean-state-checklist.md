# 局部收尾检查清单 (Clean State Checklist)

## 1. 代码质量
- [ ] **构建检查**: `go build` 无错误。
- [ ] **测试验证**: 运行模块级测试命令并通过。
- [ ] **代码清理**: 移除了所有 `fmt.Println`、硬编码的调试值及未使用的变量。

## 2. 文档与状态
- [ ] **任务同步**: `feature_list.json` 状态已更新。
- [ ] **进度同步**: `progress.md` 已详细记录当前存档点。
- [ ] **契约同步**: 如果修改了对外接口，已同步更新根目录下的 `CONTRACT.md`。

## 3. 提交确认
- [ ] 本次修改是否符合 `ARCHITECTURE.md` 定义的 DDD 边界？
- [ ] 外部依赖是否已正确通过 Port 接口进行解耦？