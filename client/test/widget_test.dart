import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Module 1 placeholder', (WidgetTester tester) async {
    // Full widget tests in later modules
    expect(1 + 1, 2);
  });
}
