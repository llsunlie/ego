import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const Color gold = Color(0xFFD4A853);
  static const Color warmGold = Color(0xFFE8C97A);

  // Accent
  static const Color coldBlue = Color(0xFF7B9EC7);
  static const Color softPurple = Color(0xFF9B8EC4);

  // Background
  static const Color darkBg = Color(0xFF0D0D1A);
  static const Color surface = Color(0xFF1A1A2E);
  static const Color surfaceLight = Color(0xFF252540);

  // Text
  static const Color textPrimary = Color(0xFFF0EDE5);
  static const Color textSecondary = Color(0xFF8B8B9E);
  static const Color textHint = Color(0xFF5A5A7A);

  // Semantic
  static const Color cardGlowGold = Color(0x33D4A853);
  static const Color rippleGold = Color(0x66D4A853);

  // Star states
  static const Color starBright = Color(0xFFFFFFFF);
  static const Color starDim = Color(0x44FFFFFF);
  static const Color starForming = Color(0x88D4A853);

  // Chat
  static const Color pastSelfBubble = Color(0xFF2A2A40);
  static const Color userBubble = Color(0xFF1E3A5F);

  // Onboarding
  static const Color obLightBlue = Color(0xFFA0CDFF);

  // Overlay
  static const Color overlayDim = Color(0x80000000);
}
